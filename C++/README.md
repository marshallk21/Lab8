#### The program gets a number from the user and stores it in a variable
#### Displays whether that number is even or odd
#### To run the program you must first compile it and then you can execute it
